# apaz_app

> apaz

## Build Setup

``` bash
# install dependencies
npm install do the npm install also in the backend folder

# serve with hot reload at localhost:8080
npm run dev

# the SQL DB is located in apaz_app/backend/sql

# You can access as admin using user: admin@admin.com password: password
